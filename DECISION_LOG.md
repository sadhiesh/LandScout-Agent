| Date (YYYY-MM-DD) | Change / Decision | Rationale | Files Touched |
| --- | --- | --- | --- |
| 2026-09-06 | Chose facet-guided iterative narrowing over first-page parcel selection. | LandWatch already returns contextual facet counts, so broad searches can be narrowed deterministically before enrichment instead of asking users to pick from arbitrary first-page parcels. | `agents/supervisor/criteria_gate.py`, `agents/supervisor/supervisor.py`, `api/chat.py`, `tools/landwatch/landwatch/*`, `ui-next/src/chat/*`, `tests/*`, `docs/*`, `memory/*` |
| 2026-09-06 | Kept the live trace stream (`useTraceStream.panels`) scoped to the current turn only, and added a separate `history` list plus `GET /debug/sessions/{session_id}/trace` instead of re-keying the trace pipeline by `session_id` — over rearchitecting the whole `run_id`-keyed event bus/Postgres trace model. | The narration bubble, confidence footer, and Showcase Mode already depend on `panels` meaning "this run only"; re-scoping trace storage to sessions would have required touching every one of those. Archiving each finished turn's events into `history` on `beginRun()`, and hydrating `history` from Postgres on session load/switch, keeps every existing live-view consumer unchanged while giving the Observability rail the full session's reasoning trace. | `ui-next/src/lib/useTraceStream.ts`, `ui-next/src/rail/TransparencyRail.tsx`, `ui-next/src/App.tsx`, `ui-next/src/workspace/RightWorkspace.tsx`, `memory/store.py`, `api/debug.py`, `docs/agent-contracts.md` |
| 2026-09-06 | Added a `primary`/`secondary` tier to `RefinementOption` (acreage, price, location, residence are always shown; the rest are LLM-curated once the option set is wide) and a multi-select "Apply filters" action (`selected_refinement_ids`), instead of a new nested `facet_groups` contract or a bespoke UI-only reordering. | The Stage 2 LLM call already produces `recommended_option_ids` for the narrative recommendations text; reusing those IDs to also trim which secondary options render keeps the dropdown and the prose from disagreeing, with one added field rather than a new response shape. Grouping-by-section and radio-vs-checkbox layout are derived client-side from the existing flat option list, so no persisted schema or Scout/LandWatch contract changed. | `agents/supervisor/criteria_gate.py`, `agents/supervisor/supervisor.py`, `api/chat.py`, `ui-next/src/chat/RefinementPicker.tsx`, `ui-next/src/chat/ChatWindow.tsx`, `ui-next/src/lib/useSession.ts`, `docs/agent-contracts.md` |
